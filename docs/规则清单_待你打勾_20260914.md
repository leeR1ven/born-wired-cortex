# 规则清单（待你打勾）2026-09-14

你说"哪里漏了我记不清楚"。**所以别回忆了**——我把代码里实际实现的规则**全部列出来**，
每一条一行、标出处。你只需要：

- ✅ 这条是我的架构 → 保留
- ❌ 这条是 AI 自己加的 → 关掉（代码留着，默认不生效）
- ❓ 看不懂 → 问我

看不懂的直接问，不用猜。

---

## A. 基础构件

☐ **A1**　每个特征用"正、反一对"神经元表示，两者竞争，**恰好激活一个**；平局算正。
　`视觉前处理_visual_preprocess.py:15`

☐ **A2**　特征强弱用"温度计码"：L 档 = 前 L 对的正神经元亮、后面全反。
　`视觉前处理_visual_preprocess.py:31` 附近

☐ **A3**　多层网络里，每个神经元只连上一层附近的神经元（环形邻域 ±半径，首尾相接成环）。
　`视觉前处理_visual_preprocess.py:54`

☐ **A4**　多层网络的权重**出生时随机、之后永不改变**，是纯信号处理电路，不做学习。
　`架构说明.md` 第 1.2 节

☐ **A5**　视觉最后一层会自动校准，平均每帧亮的细胞数稳定在 ~240 个。
　`视觉前处理_visual_preprocess.py:105`

☐ **A6**　可塑连接的管理：用一次加权重；连接太多时定期整体衰减；太弱就删掉。
　`权重连接管理_weight_manager.py:21`

---

## B. 时间

☐ **B1**　全系统只有**一个**共享时钟（时间环），所有记忆区都引用它。
　`海马体时间区_hippocampal_time.py:38`

☐ **B2**　时间环 1728000 格，每格 0.05 秒，绕一圈 24 小时；一帧 = 2 格 = 0.1 秒。
　`海马体时间区_hippocampal_time.py:4`

---

## C. 记忆区（视觉/听觉/运动三个区）

☐ **C1**　上一帧的特征 → 下一格时间神经元。
　`视觉记忆区_visual_memory.py:20`

☐ **C2**　上一格时间神经元 → 下一帧的特征。
　`视觉记忆区_visual_memory.py:24`

☐ **C3**　回忆：给一组特征 → 各时间细胞按权重加票 → 得票最高的时刻胜出 → 沿时间环连续播放后续帧。
　`视觉记忆区_visual_memory.py:32`

☐ **C4**　三个区各有一套记忆，共用同一个时钟。
　`架构说明.md` 第 2.6 节

☐ **C5**　运动记忆区记的是"大脑发出的命令序列"，不是每块肌肉的发力。
　`运动记忆区_motor_memory.py:10`

---

## D. 前额叶

☐ **D1**　前额叶输入宽度 = 视宽 + 听宽 + 运宽（自动联动，不用手改两处）。
　`前额叶区_prefrontal.py:530` 附近

☐ **D2**　中间层用一个阈值、输出层用更高的阈值，**保证只亮少量神经元**。
　`前额叶区_prefrontal.py:67`

☐ **D3**　输出层里有一批"**汇合细胞**"，额外接收另外两路的**出生固定**长程线，阈值也抬高
（只有多路信号一起来时才放电）。
　`前额叶区_prefrontal.py:71-90`

☐ **D4**　**天生互惠返回线**：把前额叶的输出沿出生固定的镜像连线回送给三个感觉区。
固定权重、不学习、永不遗忘。
　`前额叶区_prefrontal.py:135`、`:612`

☐ **D5**　**海马索引**：只记"哪一刻"，不存内容；同一对只记一次，**重复经历不加重**（学一次就记住，不越记越模糊）。
　`前额叶区_prefrontal.py:162`

☐ **D6**　锁定时刻时，要"票最多**且明显领先第二名**"才算数；票不够就算"没见过"，不乱回放记忆。
　`前额叶区_prefrontal.py:315`

☐ **D7**　**念头 → 念头**的联想学习（在联想区内部）。
　`前额叶区_prefrontal.py:341`

☐ **D8**　抑制神经元压它附近的念头细胞；去抑制神经元专门压抑制神经元（门一开，附近念头容易亮）。
　`前额叶区_prefrontal.py:333` 附近的说明

☐ **D9**　**动态整体抑制**：这一微步亮的念头细胞太少就放松、太多就收紧，把数量压在目标区间。
　`前额叶区_prefrontal.py:474` 附近

☐ **D10**　一个念头后面跟过多个后继时，**少量候选并存**，而不是只留唯一一个。
　`前额叶区_prefrontal.py:749`

☐ **D11**　**专注**：思考时把三条通路压低（外界输入、回忆、运动输出）；没在想事情时三条通路默认全开。
　`前额叶区_prefrontal.py:816`

☐ **D12**　专注不是瞬间开关：念头一来要几个微步才压下去；念头一停逐步恢复。
　`前额叶区_prefrontal.py:513` 附近的说明

---

## E. 运动输出

☐ **E1**　肌肉用"发力温度计"，每块肌肉 0~10 共 11 级发力。
　`运动输出区_motor_output.py:1`

☐ **E2**　**反向赫布网络**：命令码 → 肌肉特征，**一次学会**（权重到 1 就饱和），学会后永不遗忘。
　`运动输出区_motor_output.py:163` 之后的新版

☐ **E3**　**门控省位**：先自测召回，已经接近真实肌肉就不再新增连接——反复做同类动作不会把记忆铺满。
　`架构说明.md` 第 2.3 节

☐ **E4**　**天生镜像底版**：召回时叠一条固定镜像线的微弱投票，让没学过的全新命令码也能给出肌肉的大致位置。
　`运动输出区_motor_output.py:144`、`:316`

---

## F. 你刚补的——本能连接到底覆盖什么

你刚才说：本能连接**既包括听觉区/视觉区/运动区皮层一开始就有的连接，
也包括前额叶区皮层一开始就有的连接，范围挺广**。

☐ **F1**　每个感觉/运动区（视觉、听觉、运动）的皮层，**一开始就有连接**。
　⚠️ 现状：骨架里每个区的**多层网络**确实是出生就有的固定连接（A4），
　但**区与区之间**没有出生就有的连接（只有前额叶那批固定的"汇合细胞"长程线，D3）。**是不是漏了？**

☐ **F2**　**前额叶区皮层一开始就有连接**。
　✅ 现状：有（前额叶的多层网络 + 天生互惠返回线，D4）。

☐ **F3**　各区之间**既可以直接连**，也可以**经过前额叶间接激活**。
　⚠️ 现状：**只有"经过前额叶"这一条**（汇合细胞的固定长程线 + 天生互惠返回线）。
　**区与区之间的直接连线，骨架里没有。**

☐ **F4**　**经常互相激活的神经元，可以跳过前额叶直接激活**（也就是说：这种"直连短路"是**后天学出来的**，
　不是出生就有的）。
　⚠️ 现状：**这条规则骨架里完全没有**。唯一沾边的是运动区的反向赫布网络（E2）——
　它确实是一条"学出来的直连"，但只存在于"命令码 → 肌肉"这一处。
　**你要的应该是：任意两个经常一起激活的神经元之间，都长出这样的直连。**

---

## G. 我确认是 AI 自己加的（你不用判断，直接说"关掉"或"留着"即可）

☐ **G1**　习惯化：细胞带"疲劳值"，刚放过电就变难放。　`full_model/pfc_habituation.py:15`
☐ **G2**　新奇：没见过的颜色组合给更高分。　`full_model/pfc_novelty.py:15`
☐ **G3**　序列：额外一批细胞，把两幅连续画面绑起来并记住中间的动作。　`full_model/pfc_sequence.py:28`
☐ **G4**　紧张性运动：一直保持基础肌肉张力（站着不倒）。　`full_model/pfc_tonic_motor.py:24`
☐ **G5**　深思闸门：先在心里多比较几步再动手。　`full_model/pfc_deliberation.py:15`
☐ **G6**　目标区：记一个"目标"，用它影响选择。　`full_model/pfc_goal_update.py` / `pfc_shared_goal.py`
☐ **G7**　运动抑制：一个动作压住相反的动作。　`full_model/pfc_motor_inhibition.py`
☐ **G8**　前额叶联想区 / 专注闸门 这两个**名字**（机制本身你认，D7~D12 是机制）。
☐ **G9**　`反向赫布网络旧版`（没人用的旧代码）。　`运动输出区_motor_output.py:163`
☐ **G10**　`锁定全键`（没接上的加速代码）。　`前额叶区_prefrontal.py:253`

---

## 请你只回答三类

1. **A~E 里哪些是 ❌**（不是你的架构）
2. **F1~F4 我标了 ⚠️ 的四处，确认一下是不是漏了**（尤其 F4，我觉得是漏得最厉害的一条）
3. **G 里哪些直接关掉**