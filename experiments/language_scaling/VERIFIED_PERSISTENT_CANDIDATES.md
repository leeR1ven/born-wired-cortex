# 持久多候选实验独立核验

状态：PASS。核验对象为 persistent_candidates_v1 与 candidate_budget_v1，使用已保存权重，不重新训练。

独立字典与 NumPy 状态更新逐项复现 9,472 行预测、112 条完整生成/静默轨迹共 7,168 步，核对候选注入、全部持久活动、概率与名次、活动汇总、尾部周期和集合 Jaccard；检查 20 项冻结来源哈希、验证集选择及测试位置排除。动态复核耗时 90.17 秒。

原始核验证据在 candidate_coverage_audit.json 的 persistent_dynamics_verification 字段；详细分析在《候选覆盖核查.md》。本文件仅补齐报告引用名称，不代表新增实验或重新核验。

未发现当前实现的因果或状态更新错误。候选从旧状态产生，随后才接入真实字或自主输出；静默模式不将赢家设为 1。100k 模型的换行偏向可独立复现，其中换行自连接贡献了示例总换行电流的约 81%–89%，并非显示编码错误。

第一轮与预算轮测试位置不同，不能将 56/512 与 79/512 直接解释为配对提升。预算轮同一批 512 个位置：基线命中 85，弱候选 79，强候选 45。弱候选仍出现五字循环；活动数量扩大不能单独证明语言能力改善。

distinct_final_silent_top8 统计有序前八序列，不能解释成不同无序字符集合。以上结论只适用于这里的简化字符图。
